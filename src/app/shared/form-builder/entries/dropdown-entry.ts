import { BaseEntry } from './base-entry';

export class DropdownEntry extends BaseEntry<string> {
  controlType = 'dropdown';
  options: {key: string, value: string}[] = [];

  constructor(options: {} = {}) {
    super(options);
    this.options = options['options'] || [];
  }
}